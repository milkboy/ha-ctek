force_refresh:
  target:
    device:
      integration: ctek
  fields: {}
send_command:
  target:
    device:
      integration: ctek
  fields:
    command:
      required: true
      example: REBOOT
      selector:
        text:
